package com.example.vmg.controller;

public class StatisticalController {
//    select s.id, s.name, s.code ,count(s.id) as quantity from staff s, welfare_staff ws
//    where ws.id_welfare = 2 and s.id = ws.id_staff
//    group by s.id
}
